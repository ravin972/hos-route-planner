# Reference only: Vite reads real environment variables prefixed with VITE_. Never commit real values.

# Origin of the Django API, without a trailing slash.
# Leave it empty in local development: the Vite dev server proxies /api to http://127.0.0.1:8000.
# In production set it to the backend's public origin.
VITE_API_BASE_URL=
